# Task2b: actual dynamic feedback runtime and FP32 decoder

Read this first; only this bounded subtask. Worktree task7-fp32-feedback/project cosmos_umi_capstone, user Luna xhigh implementation/main independent review. No subagents. No live model/GPU calls until review. Prior untracked umi_task7_runtime.py and test_umi_task7_runtime.py are UNAPPROVED scaffolds; replace incorrect parts freely without touching historical Task4-6 results. Do not edit runner draft: operational runner is next Task2c.

Task2a approved API OfficialPrecisionRuntime.execute(spec,inputs,scope='full',decode_policy='deferred'), still30 real steps, zero decode, validated real G/conditions/noise/scheduler. Encoder Task1 approved FeedbackEncoder(tokenizer).encode(frame,precision='native'|'temporary_fp32') returns arrays actual_output condition-only and dtype/restore evidence. Use these, not imaginary backend.step or scope=module. Main earlier draft findings docs/task7-task2b-draft-review.md items1-4,9 apply to this scope.

Implement actual FeedbackRuntime in experiments/umi_task7_runtime.py (separate decoder module only if clarity demands). It receives ONE loaded OfficialPrecisionRuntime and frozen historical full z0, runtime mask/indexes,v0 plus encoder. No model loader here, no second resident model. Public step(condition_only,step_index) accepts shape matching VAE condition-only tensor; step_index only0 or1, seeds[0,1]. Provide explicit extract_condition(fullcarrier)/embed_condition(condition_only) helpers from runtime temporal mask/indexes, preserving full non-mask exterior z0 bytes. Reject wrong/mismatched shapes instead of flattening channels/time blindly. Source z0 is immutable Task6 value, not reencoded initial image.

Use request-local shallow runtime binding sharing model/ops but cloning prepared tuple/control metadata, binding a real dynamic inputs object and request seed. inputs.identity must agree with bound runtime.inputs; for_spec returns exact requested fullcarrier. No original frozen z0 reused in mask. Update clean.x0_tokens_vision, reference and initial condition slots independently each request. Prediction noise uses ACTUAL installed official arch_invariant_rand as used by model._prepare_inference_data, with original fullcarrier shape/dtype/device, then official flatten/blend layout. Resolve function from actual official source/imports, don't guess module. Seed0 noise exact equality to frozen Task6 original outside mask is hard assertion; per-seed actual noise hashes stable and seed0/1 distinct. Never hash condition-including initial state as prediction-only noise. Store function source/strategy identity, actual prepare/sampler/scheduler seed observations.

After full/deferred return cleanup, perform exactlyONE FP32 decode thenONE FP32 encode. No diagnostic fallback or retries. Decoder receives output_full, NOT predicted-only slice. Quantitative predicted_latent remains separate. Implement actual FP32 D proof analogously to approved E: cast same loaded VAE weight/buffer/constants, no recovering lost bits; wrap inner decode compute with positive-count dispatch observer AFTER precision conversion and BEFORE restoration (avoid counting legitimate BF16->FP32 weight conversion as hidden compute). Inspect boundary input/output and all floating state. Disable autocast/TF32 with reliable finally restore; eval/inference mode; caches clear. Can factor/reuse tested Task1 precision helpers rather than duplicate state machinery. One model.decode invocation counted at real boundary. Record raw complete decode or last generated RGB and slicing. Normalize to same float32 [0,1] P as Task6 FP32D helper (float64 arithmetic then float32 storage if needed for exact parity); explicitly log clamp fractions/layout/range/no resize. NumPy normalization is preprocessing, not disguised model compute precision.

Encoder gets exact normalized CHW frame; record original actual_output condition-only, embed into fullcanonical carrier separately. All G/D/E evidence preserved JSON+independent numpy CPU copies. step output includes condition_input, full/predicted G latent, raw/normalized lastframe, encoded_condition, encoded_carrier, actual prepared/first/last/per-step conditions, prediction-noise hash, actual G/D/E counters, elapsed/peaks. Record arrays finite FP32, maskbool. No arrays of32runs retained. Errors expose partial capture/counters, restore every hook/cache/dtype and don't autotry. Cleanup must include errors during invoke itself, not only after it returns. No fake defaults for missing operation evidence. Reports must distinguish current step's input consumption from encoded next output, which is checked by runner at next real invocation; final z2 no third generation.

TDD on existing actual OfficialPrecisionRuntime fakeOps fixture, extending only concrete ops where required: two seeds reach30step sampler and scheduler, equal same-step noise/different acrosssteps, dynamic feedback differs from initial condition and is exactly consumed at all30steps, mask scatter preserves temporal/channel coordinates, fulllatent passed once to decoder, actual encoder shape accepted, failure restoration, no stale original conditions, no diagnostic module path. Real Torch CPU decoder/encoder fixture proves precision/cleanup separately; fake outputs alone never establish real GPU evidence. Focused tests first, no broad missingdependency suite repeats. Remote CPU only upload available through SSH seetacloud-umi require_escalated, CUDA_VISIBLE_DEVICES empty, existing interpreter /root/autodl-tmp/cosmos-framework/.venv/bin/python; no model load.

Commit runtime/decoder/scoped tests and root docs/task7-task2b-report.md. Report exact API/schema for Task2c, complete test commands/outputs and genuineRED. Do not claim wholeTask2 or live stages complete. Read root docs/task7-source-review.md for actual remote paths/source clues and frozen hashes. Raise concrete blocker before inventing shortcuts.
