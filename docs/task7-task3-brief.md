# Task 3: offline Task7 analysis and artifacts

Pending main release after runtime schema review. Luna xhigh implementation, main independent review. No remote generation or subagents. Read Task2 report for actual immutable sample schema, not inferred file names. Implement experiments/analyze_umi_task7.py plus focused tests. Preserve prior analyzers.

Use tensor-only quantitative data. Float32 subtraction, float64 RMS/cosine/regression reductions. Quantitative norm at feedback interface is condition-mask RMS, not full carrier RMS with zeros diluting the result. Validate shapes, finite values, masks, input/source hashes, exact consumption and noise identities before science. Engineering failure is distinct from valid scientific FAIL. Load one sample or pair at a time; explicitly close mmap before moving on. Never rewrite successful raw data.

Stage A: eight original perturbation inputs, same saved FP32-D frames encoded native/FP32. Verify native equality to Task6 saved direct condition. Compute each encoder's own baseline floor. Compare complete z0 -> FP32G -> FP32D -> E response versus actual original z0 perturbation RMS. This is NOT an arbitrary RGB-space directional derivative. RGB/input preprocessing delta is separate diagnostics. For each of three alphas and both signs compute effective step, target direction cosine, opposite cosine, outside-mask exactness, response RMS, noise ratio, gains. Zero floor ratio may be null with explicit NONZERO_ABOVE_EXACT_ZERO flag, never arbitrary infinity serialized as JSON. All three points required, no dropping smallest amplitude.

Use actual-step central secant q=(F_plus-F_minus)/(h_plus+h_minus), where h_sign is mask RMS of actual input change. Adjacent relative change is RMS(q_next-q_current)/RMS(q_current), matching Task5 denominator convention; cosine >=.95, change<=.25. Fit positive/negative response RMS against their own actual h in log-log: slope[.8,1.2], R2>=.98. Inputcos>=.99, opposite<=-.99, no exterior changes, response>10*floor (strict >0 if exact-zero floor). Any undefined value fails reliability rather than silently passes. Native/FP32 gate separately, additivity NOT_TESTED. Three alphas define single prespecified candidate window.

Stage B: per-step independent baseline-pre/post floor (z0,z1,z2); all six perturbation trajectories retained. Compute delta0,delta1,delta2 against corresponding baseline_pre at same interface; A1/A2=RMS(delta1/2)/RMS(delta0), incremental ratio A2/A1; zero denominator N/A. Save delta tensors. Report positive/negative asymmetry descriptively, avoid inventing extra thresholds. Require actual feedback encoded condition equals next consumed input and per-step prediction noise hashes match all trajectories; seed0/seed1 differ. First-step G must match Task6 source for each sample. Report final z2 as actual prepared next-condition coordinates; do not imply a third generation was performed. Baseline exactness gates C. B finite propagation remains valid despite A scientific fail but not engineering failure.

Stage C: six separate measured delta1 rays (no original-v0 projection). Each probes at beta .1,.2,.4; actual input geometry and response floors separately verified. Same gate as A around shared z1 baseline and seed1, across all three betas. Fixed q at beta.1 predicts delta2=q*RMS(delta1); Eprop=RMS(actual_delta2-pred)/RMS(actual_delta2). <=.10 PASS only when all local gates and measured response >10*floor (or nonzero at floor0) hold. Otherwise UNRELIABLE with Eprop optionally descriptive, not counted as PASS or omitted. Zero direction/response N/A. C skipped if A not PASS or B wiring/repeatability false. Require 38 C calls when C complete; no actual-generation claims from synthetic tests.

Write analysis JSON with engineering/science/stage status and counts; A comparison/geometry/secant CSV; B propagation/floors CSV; C all-six prediction/local gate CSV if executed; explicit skipped C otherwise. PNG+SVG charts A response/precision comparison and B A1/A2 vs alpha including +/-; conditional C errors. Reuse available plotting helpers without new dependency installations. Include math note with conditional differentiability assumptions and O(h^2)+O(u/h), referring docs/task7-mathematical-interpretation.md. Report controlled repeated action, seeds[0,1], no accuracy/global stability/rank claims. Manifest excludes itself, light review zip excludes large raw arrays/models/keys/video. Preserve remote raw provenance paths/hashes. CLI can analyze completed A before B, never requiring unavailable future stages.

TDD: linear affine synthetic F0,F1 recovers known A1/A2 and Eprop (including differing actual delta1 ray); smooth nonlinear local failure; zero denominator; high floor; quantization disappearance; mismatched mask/hash/shape/noise; positive/negative asymmetry; stage C preconditions; strict three-amplitude fit; all six directions retained; mmap closed on success/error. Provide independent hand-computable linear fixture. Report exact commands and genuine RED/GREEN outputs, no fake runtime evidence presented as live GPU proof. Commit scoped code/tests, report docs/task7-task3-report.md and schema/CLI for main review.
