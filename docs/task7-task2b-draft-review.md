# Unapproved Task2 draft: findings to resolve after Task2a

This is a preliminary architecture review of untracked files, not final code review. Task2a currently owns only real official deferred-decode seam. Do not run these drafts on a model.

1. Full30 vs diagnostic module: _official_module_step is invalid; use Task2a full/deferred with all scheduler steps and validated G evidence. Do not default missing observed G dtype to ['float32'].
2. _DynamicInputs identity must truly bind current target and runtime.inputs coherently; current execute checks inputs.identity()==self.inputs.identity(). No changing seed metadata while using stale prediction noise. Use carrier-shaped official random draw before flattening (not packed template shape unless verified exact official construction); freeze/hash prediction-only region, not initial whole state containing changing condition.
3. Encode returns condition-only [1,48,1,16,16], not fullcarrier [1,48,5,16,16]. Keep original encoder result, scatter via runtime condition index/mask, preserve canonical exterior; exact actual next consumption comparison. Never use reshape pretending temporal coordinates coincide.
4. Current _decode_once selection can prefer predicted slice while demanding fullcarrier; real D consumes FULL sampler latent, quantitative predicted-block slice separate. Decoder dispatch observer around whole replay also observes precision conversion/restoration, so measure actual decode compute inside conversion context rather than reject legitimate boundary casts. Preserve exact Task6 FP32-D normalization for A/B parity, record P.
5. Resource gate draft is not acceptable: host available<10GiB replaces approved<300GiB, swap_free<1 replaces swap_used>0, wrong cgroup key vs Task6, missing RSS/growth gates, missing snapshot can pass, warning can return before disk hardstop. Reuse Task6 evaluate_resources/sampler/latched ResourceMonitor with required complete actual snapshot, not an independently weakened rewrite. Match smoke/preload/forecast gates precisely.
6. run_stage permits monitor=None, missing counters inferred as success, skipped samples counted as completed but calls reset0, resume COMPLETE returns without artifact revalidation, identity binding arbitrary/possibly empty. Require strict full identity including allTask7code, actual source artifacts and provenance; observed cumulative attempts/success/calls; validate immutable artifacts before resume skip/complete.
7. No actual source-bound A/B/C executor or runtime loader wired to CLI yet. Need real source8 mapping, A native parity, B ownfeedback and firstG historical parity, C actualdelta1 rays and stage gates. CLI currently just writes preflight placeholder; full requirement remains operational stage CLI.
8. Atomic store deletes failed temp dirs with rmtree; user requires retain failed attempts independently. Preserve partial evidence with error/status and numbered attempt. Monitor/serialization errors must record status and release caches, close mmap, no autotry. Protect with processlock, one runtime.
9. Noise/action/seed identity observations must come from actual underlying capture, not spec labels. Encoded z1 equals actual step2 prepare/first/last; terminalz2 is not a third generation.

Resolve these in bounded2b implementation. The original root docs/task7-task2-brief.md is still binding; do not limit work to satisfying superficial fake-seam tests.
