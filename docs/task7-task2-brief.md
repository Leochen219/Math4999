# Task 2: real feedback runtime and staged runner

Pending Task1 review; do not implement before main releases Task2. Work in this worktree's cosmos_umi_capstone. Use approved Task1 encoder seam (report API is authoritative) and existing Task4/6 runtime, resource monitor and atomic storage.

## Runtime contract

Create experiments/umi_task7_runtime.py for FeedbackRuntime.step(condition, step_index), consuming exact condition coordinates plus known runtime mask. Return float32 condition input, full/predicted G latent, raw/normalized Decoder last frame, encoded next condition and evidence (actual prepare/first/last conditions + per-step hashes, noise hash, G/D/E actual operation dtypes/counts, elapsed/memory). No another resident model.

Existing OfficialPrecisionRuntime is bound to seed and prepared tuples; both must be refreshed consistently, with fresh request-local control state sharing ONLY loaded model/ops. Never simply change spec_seed. Use installed official arch_invariant_rand for each seed and verified shape/device/dtype; validate against official prepare algorithm or call official preparation with a request-local clean-data override so it performs no unwanted encode. Stage0 seed0 prediction noise must match Task6 original; stage1 seed1 differ yet exactly match all seed1 trajectories/C probes. Do not assume noise hashes differ by changing metadata only. Task4/5/6 defaults unchanged; an optional decode policy seam is acceptable if backwards-compatible and tested. Exactly ONE actual FP32 decode per F; do not native-decode then replay and undercount.

Every call injects requested condition into clean carrier/reference/initial slots, keeping exterior canonical; no original-z0 fallback. Masks from real runtime and exact equality with Task6. Saved encoder output must equal next real consumed condition. Strictly distinguish full carrier [runtime shape] and condition-only [runtime mask shape]; no blindly flattened reshaping across temporal/channel positions.

FP32 Encoder/Decoder computational proof follows Task1. Restore hooks, flags, native dtypes and request caches even on failure; cleanup finally. Full quant arrays independent CPU copies, don't hold all outputs. First-step G output exact vs corresponding Task6 sample before declaring success; Task7 z0 frozen source rather than recomputed initial encoder result.

## Stage plans/runner

Create experiments/run_umi_task7_experiment.py and supporting storage module only if needed to keep responsibilities focused. CLI stages preflight/smoke/A/B/C, explicit run-dir/resume/raw-root/decoder-root/model/framework/vae/assets and launch evidence. Build one model for requested stage, never parallel. No automatic next-stage transitions without recorded main gate approval. Formal immutable counts A16E; B16G16D16E; C38G38D38E. Engineering attempts separate.

A source: raw run07 plus decoder_cleanup01 temporary_fp32 replay frame and historical direct_condition_latent_float32, eight records baseline_pre/sixv0/baseline_post. Native and FP32 E on same frame; native exact compare, source hashes mandatory. A input geometry is original z0 perturbation, NOT arbitrary RGB direction; record image delta diagnostic separately. No G/D in A.
B order baseline_pre then v0 alpha00 plus/minus, alpha01 plus/minus, alpha02 plus/minus then baseline_post; 2 steps seeds0/1 each, identical action repeated. Only delta0 injection. A scientific FAIL permits B; engineering FAIL prohibits. C requires A PASS and B exact baseline repeatability/wiring. Six directions are each actual delta1; beta [.1,.2,.4], +/- per direction, shared baseline_pre/post at z1 seed1. Nonzero deltas mandatory, otherwise C SKIPPED with reason rather than fabricated estimates. Estimator beta.1 preselected, no automatic scale changes. Analysis API integrated after Task3; runner may emit data ready for analysis but must not invent passed gates.

Reuse resource sampling/thresholds: GPUpre<=1GiB; smoke peakallocated<=35/NVML<=45; hard GPUused>75/free<20/reserved>65 or existing cleanup-growth; cgroupfree warn20/stop10; existing RAM/RSS/swap. Diskpre>=10GiB, estimate1.3*actualbytes*remaining +5GiB; gate each stage/sample, no deletion. Latched async monitor error stops. Serialize then hash then free arrays, close mmaps. Keep monitor alive during cleanup; atomic run_status includes actual counters/completed/errors/lastresource/identities. Strict resume source/code/config/model/VAE/framework/mask/z0/v0/noise hashes; never mutate source Task6 statuses. Success artifacts immutable and verified on reuse; incomplete attempt renamed to numbered safe directory. Code binding must include ALL Task7 source files, not just Task6_CODE_SOURCES.

Reference remote metadata and source paths in docs/task7-source-review.md, but read real required code. No server model run/upload until main release. No subagents. TDD for dynamic condition/noise, exact consumption, decode count, stages, guards, resume and failures; focused then full suite once, report skips/errors honestly. Commit scoped code/tests. Report docs/task7-task2-report.md with API for Task3 analyzer and invocation CLI, RED/GREEN and full suite commands/results. Raise architecture uncertainties; do not pretend fake-runtime unit tests establish real GPU validation.
