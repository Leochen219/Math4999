# Scoped main re-review of 9839171 — remaining fixes, round 3

Main runner32/32 PASS2.066s. No GPU release. Addressed: actual ResourceMonitor integration, direct Torch telemetry/peak-reserved/reset hooks, initial-A conservative forecast, staged publication API, A error counts, full-carrier B conditions, C encoded-z2 parity/nonfinite rejection, explicit skip classification, monitor stage/attempt paths and dead legacy branch removal. The following are residuals in the fixes, not an invitation to redesign.

1. **Actual output lifetime**: run_stage creates payload={record:value}; del value does NOT release record because payload still references it during release_sample/post_cleanup and into next execute. Delete payload too after stage_success. Resumed prior_record also survives continue into next call; release it. Add a weakref-to-output-array test asserting object dead inside cleanup callback, not merely that callback names appear in order. Check sample copies disappear after gc as intended.

2. **Live accounting still incomplete**: smoke post_call monitor/peak gates occur outside the catch preserving returned counts; if a peak gate fails, root says G/D/E=0 although feedback.step ran. After successful smoke, monitor.stop/unload failure also gets zero counts and smoke/run_status remains SMOKE_COMPLETE (_mark_stage_cleanup_failure only checks stages/SMOKE). Formal failure root reconstructs a new terminal exception without stage attempt_counts, so root counts zero although stage knows them. Keep explicit live observed counters through all these paths, preserve both stage and root terminal evidence, and count actual setup encodes separately (currently no setup counter/evidence exists anywhere). Minimal setup observer can wrap the existing factory.loader return: payload encoder.encoder is the actual tokenizer and OfficialRuntimeFactory.build constructs OfficialPrecisionRuntime after loader returns; observe encode calls at that boundary and restore method ownership in finally. Audit loader before deciding scope; do not invent a zero/one count. Include setup boundary failure evidence separately from formal counts.

3. **Strict COMPLETE resume**: formal binding doesn't save launch_contract_sha256. _precheck_resume compares only some contract fields; changed prompt/settings with unchanged model/VAE is accepted before _validate_source_contract. Validate the full current source/action/contract and path identities on fast path, bind the full contract hash for formal stages, and recompute observed/attempt/completed counters from verified sample status + failed attempts rather than trusting declared totals. Duplicate completed/skipped IDs and overlaps must reject. Prefer reusing existing pure verification logic rather than a third slightly different validator. Smoke without --resume must be rejected before build if success exists; terminal root failure must prevent repeat with or without --resume (new run directory is required). No expensive duplicate inference just to fail publication.

4. **C gate lineage**: gate currently only binds generic source/code. Add current A/B completion evidence digests (run_status + sample manifests) to required gate schema and compare before C. This is small and will be produced by the next analyzer task; document exact schema for that task. Do not expand the scientific thresholds or call plan.

Tests: genuine weakref release; smoke returned G/D/E followed by post-peak failure and shutdown failure; formal root accumulated counts; setup encode observer success/failure/restoration; COMPLETE prompt/contract/counter/duplicate tamper; smoke second launch without resume has zero factory/G calls; foreign A/B gate rejected. Freeze scoped code/tests/report with exact result. Keep corrections minimal; no old Task4-6 changes, GPU or upload.
