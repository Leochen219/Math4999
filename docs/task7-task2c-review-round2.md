# Main review of frozen 817a63a — changes required (round 2)

Main independently ran focused 21 tests: PASS in .515 s. No GPU release. C ray source and normalization, B own-step resume, historical action hashing and protected-root handling improved. Remaining previously requested requirements are not implemented despite report language.

1. Resource lifecycle: run_stage calls post_cleanup BEFORE serialization, before del value, and never gc.collect/empty_cache or reset_peak_memory_stats. Previous value survives while next execute runs. Smoke similarly calls cleanup while holding record; no real cleanup. Implement save-to-uncommitted-attempt -> release current record/serialization copies -> actual gc/CUDA cleanup -> synchronous post_cleanup -> publish success. On cleanup failure retain failed attempt, not success. Per-call reset and actual max_memory_reserved required. Current _monitor_peak_snapshot uses max CURRENT reserved, not peak reserved. Task6 sampler still silently returns zeros on Torch exceptions throughout run: _require_live_gpu_telemetry checks only once after load, so not a fail-closed replacement. Wrap Task7 sampler to query Torch fields directly and expose failure on every sample. Keep Task6 defaults unchanged.

2. Accounting/failure identity: A encoder exception or post-encode parity exception has no capture/count. B/C post-step parity exceptions lose the returned operation_counts. Smoke parity/serialization/cleanup failure only writes minimal root status and drops invocation evidence. _record_live_failure lacks binding/counts/resources and uses factory!=None as generation_started (false evidence before any G). Count at real boundary; attach successful return evidence to post-validation failures; record actual setup encoding separately via observation. Preserve primary error AND cleanup errors on disk. Root and stage terminal state must not disagree after monitor.stop/unload fails. Add real negative tests, not mocks that prepopulate exception.capture for you.

3. Initial A disk forecast: _measured_success_bytes(A root) excludes smoke; first A has zero samples, so production isinstance(ResourceMonitor) path always raises DISK_FORECAST_UNAVAILABLE. Use measured full-F smoke as conservative initial A estimate (no extra E). Reject empty manifests (all({}) currently true); verify full schema/references. B/C same-class forecasts must remain undiluted by A. Test using actual ResourceMonitor with fake samplers, not StaticMonitor bypass.

4. C baseline compares generated full_latent only, and misleadingly calls it b_z2. z2 in plan is B step1.encoded_condition. Bind and compare actual C encoded_condition to that B z2 as well as generated latent if useful. Reject missing/nonfinite ray/evidence; current nonfinite RMS is mislabeled ZERO_RAY and C mask extraction exceptions silently become all-ones. Valid all-zero may skip; corruption must stop. Ensure complete gate consumed belongs to this run's A/B artifact digests, not only generic source/code that could match a different run. Test z2 mismatch even when full G latent identical.

5. Resume/schema: _verify binds referenced arrays but does not require stage-specific mandatory record keys; record.json can lack every scientific array and pass. Enforce A and feedback record minimum schema on publication/read, while explicit zero-ray skip has own schema. Manifest paths themselves require containment. RUNNING resume currently treats previously saved SKIPPED_C as completed and adds to completed list, so interrupted C with zero rays fails counts. Skip vs success classification must be reconstructed. Precheck smoke/run identity and --resume before loading; existing successful smoke must never be rerun then fail at publication. Refuse already FAILED/RESOURCE_STOP at run level before build, not after expensive load.

6. Tests must exercise genuine ResourceMonitor sampling/signatures and real runner<->FeedbackRuntime record schema (can use fake numerical model). Add A count errors, serialization/cleanup/stop failures, initial A forecast, actual post-cleanup object release order, peak reset/peak reserved and late Torch telemetry failure; B full16 + interrupted own trajectory, C feedback-z2 parity and skip resume. Remove unused _legacy_main live branch so two contradictory implementations don't coexist (retain only preflight route). Do not claim all fixed until these paths have evidence.

Keep changes in runner/tests/report, no old Task4-6 production changes. This is fix round 2; freeze commit and accurate test transcript when done. Do not upload or execute GPU.
