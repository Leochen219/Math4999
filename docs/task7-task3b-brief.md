# Task3b: Task7 saved-evidence analysis, plots and report

NOT RELEASED until Task3a numerical API is approved. Luna xhigh implementation, main review. Same worktree. No subagents, generation, model loading, installs, or raw-source mutation.

Read docs/task7-task3-brief.md as full requirements; read task3a-report.md for the approved numerical API and latest task2c-report.md for exact record/gate schema. Add file loading, engineering evidence verification, stage-specific CLI, CSVs/PNG+SVG/Markdown report/math linkage/light bundle to the existing analyze_umi_task7.py and its tests. Reuse approved Task7SampleStore and completion_evidence_digests APIs, never guess .npy filenames. Preserve the reviewed Task3a mathematics.

Analyze A independently before B exists; then B with A cross-stage parity; then C if executed, or explicit skip with reason. Validate actual dtype evidence, source parity, byte-exact consumed conditions after authoritative slicing, paired seeds/noise, sample IDs/counts and original sources BEFORE science. Compare B first-step G/D/E with Task6/A, C baseline input/output with B z1/z2. Use already saved tensors only, one full sample or pair at a time; keep only small extracted response vectors needed for the three-amplitude comparison. Close mmap on success and error. Save response/delta tensors for audit, no SVD.

Produce analysis_gate.json only when A scientific and B engineering/repeatability/cross-stage gates permit C, binding current source/code and the runner's exact A/B completion digest fields. Do not manufacture a passing gate from flags alone. If prior phases are incomplete/failed, report that without treating missing outputs as zeros or launching anything.

Report English axes if fonts are unavailable; Markdown may be Chinese or clear bilingual. G/D/E neural FP32 and P's recorded floating conversion distinguished; controlled repeated-action/seed[0,1] label mandatory. No accuracy, Jacobian existence, global stability or rank claims. Preserve raw files and existing successful analysis destinations; use explicit new analysis directory or strict resume/no-overwrite. Manifest excludes itself. Light ZIP excludes models/keys/videos/large full-chunk arrays. Plots may use existing remote /root/miniconda3/bin/python (matplotlib); do not install into the experiment venv. CPU tests use temporary synthetic stores with real approved record schemas, not fake evidence in production.

Test complete synthetic A/B/C flow, A-only operation, scientific FAIL vs engineering failure, all-six C results, wrong gate lineage/source/noise/byte conditions, tampered artifact/count, mmap closure and no raw overwrite. Render representative synthetic PNG/SVG using existing dependencies before claiming plots verified; synthetic outputs labeled not-model evidence. Update docs/task7-task3-report.md with exact CLI, output schema and verification. Commit scoped module/tests/report only.
