# Task2c main review — frozen draft 4e38bab

Spec: NOT APPROVED. Quality: changes required. Seven runner tests pass (main independently ran 7/7 in .046s); they do not exercise real CLI wiring or stages. Worker aggregate 20 includes dependency tests and two skips. No GPU/model release.

The following are implementation defects, not matters that should first be discovered by live GPU smoke. Fix together, with genuine failing tests then passing tests. Preserve Task4-6 files/defaults. Do not broaden scope.

## 1. Lifecycle, resource monitoring and smoke acceptance (blocking)

main builds outside final cleanup, and suppresses monitor.stop failures. A load/identity error can bypass unload/status; monitor failure at shutdown can still return success. Lock is per-stage and acquired after model load, so cannot protect one resident runtime. Wrap entire preload/build/stage/unload/monitor lifecycle under a run-wide process lock before loading, retaining primary plus cleanup errors and recording stop with identities/counts/resources. All failure exits need unload and status; no silent except/pass.

No call to ResourceMonitor.capture_sample exists: no synchronous pre/post/post_cleanup records, no growth baselines, no per-sample peak reset/peak-reserved evidence. Background Task6 NVML field gpu_peak_nvml_used_gib is current used, NOT accumulated peak. Smoke checks limits before, never after G/D/E, and publishes SMOKE_COMPLETE without historical first-G parity or peak approval. Reuse Task6 monitor correctly, compute real accumulated peak from interval rows, record pre/post/post_cleanup and check latched decisions after each call and after final call. Actual Torch telemetry failure currently becomes zero through reused sampler: override this Task7 sampling boundary fail-closed without modifying old default. Include allocated/reserved/peakallocated/peakreserved. Reset peaks before each sample, record before reset next. Smoke must verify actual G == source output_full, precision/noise/evidence and measured size then post-call resource gate before acceptance. Guard model cleanup even smoke publication fails. Setup encoding must be counted separately by observed wrapper, not assumed zero/one. Production env HF_HUB_OFFLINE=1 and GPU0 must be explicit; no other GPU accepted.

## 2. B feedback, memory and partial resume (blocking)

make_stage_executor retains each whole record in trajectories, violating one-current-sample memory rule. Resume skips step0 without repopulating that map, so an interrupted trajectory cannot continue step1. Load the verified step0 encoded_condition from this run's immutable sample store for step1, do not retain all records. Verify equal bytes between that encoded condition and next prepared/first/last/per-step conditions (mask coordinates), and prediction-region noise hashes paired for each seed across trajectories. Enforce first-G historical parity for all eight source names. Record mismatch as engineering stop with observed invocation counts. Free current sample/serialization copies after atomic publication and cleanup while monitor remains active. Tests must run all16 calls and interrupt after one step then resume without repeating step0.

## 3. C uses the wrong scientific quantity and wrong scale (blocking)

_prepare_c_source currently loads B_*_step_1.full_latent: this is second-step generated block, NOT z1. Must load step_0.encoded_condition as z1 and delta1 in condition-only coordinates. Current target applies beta*rms*raw_delta1, yielding wrong amplitude; w=delta1/RMS(delta1), h=beta*RMS(delta1), so increment is h*w (equivalently beta*delta1 subject to declared FP32 arithmetic). Use mask RMS, not full-array RMS diluted by unconditioned coordinates. Confirm direction IDs match plan names. Missing ray is corrupted evidence, not zero ray: fail engineering; actual zero measured ray is explicit skip. C baseline must bind B baseline z1, same noise/action, and compare output with B z2. Preserve all six rays and fixed beta estimator in later analysis. Tests must prove actual probe inputs and all38 calls using nontrivial two-coordinate oracle, not only count a plan list.

## 4. Source/launch identity and preflight (important)

preflight with no sources/contract currently says PREFLIGHT_COMPLETE; reject missing required production bindings or explicitly report INCOMPLETE, never completed. Verify manifest-protected task6_plan, actual runtime action/prompt/settings/environment against historical plan, not just frozen carrier. Current _default_factory action=contract['action'] is not the established Task6 contract path: parse the verified --action file, compare array hash to source inputs.action, and prompt to source inputs.prompt. Use observe_live_launch/validate_launch_contract or equivalent actual measured checks; no merely self-attested contract values for resume. Model/VAE/framework/code/config/noise policy/source must be genuinely bound. Require raw/decoder roots for live calls. Read-only preflight cannot instantiate model.

## 5. Atomic/resume integrity and accounting (important)

Successful manifest verification only requires record.json, and does not ensure every record artifact reference is hash-bound. Enforce schema-required keys and all referenced arrays present in manifest, path-contained and hash-verified, including COMPLETE resume. Recompute exact distinct sample IDs, counts, skipped status and expected plan; reject duplicates/inconsistent counts. Failures in A encode or post-call parity currently lose attempted E/G counts because those exceptions lack capture; attach actual attempted/succeeded counts at executor boundaries and retain them for smoke/setup too. Interrupted RUNNING resume must retain attempt evidence rather than zeroing it; failed/resource stops must not auto-retry. Serialization or final cleanup failure cannot count a sample as successful. Success output and source trees never overwritten.

Disk forecasting currently averages encode-only A outputs with full F outputs, which can underestimate B/C. Use successful full-feedback smoke size for B/C until enough same-class records exist; do not dilute with A. Verify nonempty artifact manifest before trusting sample sizes. Enforce stage forecast and sample checks, no fallback to unknown estimate.

## Required verification for this fix round

CPU integration through main with injected actual-shape factory/monitor, not only run_stage callback: preload hardstop means zero factorycalls; build/encode/decode/serialize/cleanup/monitor failures always unload/status and counts. Smoke latent mismatch and post-call peak violation rejected. All A16 native parity, B16 own-feedback/resume, C38 actual-ray/probe inputs/gate conditions. Source fixture matches real Task6 task6_plan/sample/decoder schema. Artifact tamper/removal, COMPLETE counter tamper, resource host/cgroup/swap/growth/forecast, mmapclose, no record accumulation. Production factory construction can be mocked at the loader seam to validate actual kwargs without GPU. Keep optional realTorch tests distinct.

Update report to state only behavior demonstrated by code and tests. Live source preflight and GPU smoke remain separate, but missing parity/resource enforcement is not an acceptable deferred live concern.
