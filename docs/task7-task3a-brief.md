# Task3a: pure Task7 numerical metrics

Luna xhigh implements, main reviews. Worktree C:/Users/hongy/Desktop/Plan/.worktrees/task7-fp32-feedback. No subagents, GPU, remote writes, installs or prior experiment changes. Implement only cosmos_umi_capstone/experiments/analyze_umi_task7.py pure numerical functions and test_analyze_umi_task7.py. Task3b will add file loading/CLI/report/plots to the same module after this numerical API is reviewed. Do not build a second runner or artifact store.

Read docs/task7-task3-brief.md for full scientific constraints; this task covers only numerical calculations and their tests. Use docs/task7-independent-analysis-oracle.md and task7-mathematical-interpretation.md. Reuse appropriate small Task5 numerical helpers, not the old whole analyzer. Input data are NumPy arrays supplied by the caller; no filesystem/source/model loading in this task.

Provide a small documented API for: float32 difference + float64 RMS/cosine; byte equality; input geometry (actual lengths, cosines, opposite cosine, exterior exactness); three-prespecified-amplitude one-direction window; two-step propagation; and the fixed beta=.1 response prediction. Accept condition mask explicitly, validate shape/dtype/finiteness/nonempty mask. Keep observed output space and input mask coordinates clear. Zero denominators produce None plus reason, not NaN/Infinity or a fabricated0. Missing/invalid engineering arrays raise a distinct error, not scientific FAIL.

Window uses actual positive and negative input lengths independently for one-sided log-log slope/R2; actual-step central q=(Fplus-Fminus)/(hplus+hminus); adjacent change denominator is earlier q. Thresholds unchanged: inputcos>=.99, opposite<=-.99, exterior0, response>10floor or strictly>0 if floor0; both slopes[.8,1.2]/R2>=.98, adjacent qcos>=.95 and relativechange<=.25. Exactly three supplied amplitudes; no dropping endpoints or selecting a different window. Return reasons and raw scalars, not only a boolean. Additivity NOT_TESTED.

Propagation: delta0/1/2 are FP32 differences at the same masked condition interface; A1=RMS(delta1)/RMS(delta0), A2 analogous and incremental=A2/A1. Keep floor/reliability flags. Prediction uses six separately supplied actual delta1 rays in caller loops; never project onto v0. Fixed beta .1 actual-step q predicts delta2=q*RMS(delta1); Eprop<=.10 is PASS only if local window and response reliability pass, otherwise UNRELIABLE (descriptive Eprop may remain). Never use beta1 evaluation to fit/select beta.

Tests: hand-computable two-coordinate linear oracle with changed second-step ray, explicit FP32 tolerances; a nonlinear failed window; zero responses/denominators; exact-zero/nonzero/high floor; rounding disappearance; asymmetric actual input lengths; wrong mask/shape/dtype/nonfinite; signed-zero byte equality; current-q denominator; no dropping failing smallest amplitude; fixed beta estimate vs tempting better beta; all six output rows retained. Pure tests require only existing NumPy. Use bundled local Python from task3 full brief (same path as prior tasks), no dependency installs.

Write docs/task7-task3a-report.md containing API signatures/data shapes, exact tests/results and commit. Commit only these scoped files; report short DONE/concerns. Main review then Task3b.
