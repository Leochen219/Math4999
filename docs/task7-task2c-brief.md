# Task2c: operational source-bound staged runner

Pending main release after Task2b schema review. Read Task2b report for actual API. Implement run_umi_task7_experiment.py (untracked old draft not approved), optional small source/storage helpers and focused tests. All initial Task7 approved spec remains binding, root docs/task7-plan.md. Main preliminary runner findings docs/task7-task2b-draft-review.md items5-8 must be resolved. Luna xhigh, no subagents or model/GPU calls until main review.

CLI --stage preflight/smoke/A/B/C, --run-dir, --resume, --raw-root, --decoder-root, --framework-root, --checkpoint, --vae, --action, --video, --launch-contract, --task5-root, --gpu-index0. Use actual validated existing loader/factory, not a CLI that only writes placeholders and demands a hypothetical external executor. Factory returns Task6RuntimeAdapter at built[0], underlying actual OfficialPrecisionRuntime is .runtime. Task7 adapter needs underlying runtime, and wrap loaded actual tokenizer with new FeedbackEncoder. Task6 factory requires actual code_bundle_sha256 in a NEW launch contract; don't edit historical contract. Bind additional complete Task7 source digest separately (allTask7modules/analyzer plus reused changed dependencies). Preserve model/VAE/framework/input hashes. Official factory performs setup initial encode; track any actual setup encode as engineering, never replace frozen sourcez0. No model or environment download/install.

Source audit validate8 samples baseline_pre,sixv0alpha00/01/02plus/minus,baseline_post. Raw samples/<bridge...>/status.json success with artifact_sha256; decoder sibling<bridge...>__temporary_fp32/record.json artifact_sha256. Verify source hashes, rawoutput_full == replaydecoder_input hash, same z0/mask/prompt/actions/seeds, actual frozenv0 from perturbation (baseline directionzero), mask/scaled condition coordinate agreement. Preserve rawroots read-only; newrun outside source roots, reject containment. Reference docs/audit_task7_sources.py is independent main audit, not sole implementation oracle.

Stage A16 encode: each frozen frame native thenFP32, eightframes; zero G/D. Actual native output exact vs historical direct_condition_latent_float32 or STOP_ENGINEERING. Store original z0/inputdelta geometry with sourcehashrefs, frame/preprocessing/actualinput/dtype/encoded condition arrays. No uint8route.
B16 full F calls:8 trajectories in fixedorder eachstep0seed0,step1seed1. Initialcondition from actual historical source consumed_input arrays (verify construct +/-alphaszv0 matches), later only own encoded_condition. Step0Gfull exact historicaloutput_full. Store step1encoded==step2prepared/first/last exact flags/hashes. All actualnoises withinseed paired. Two baseline trajectories, no reinjection/rescale. No third G. Preserve each step atomically separately; on resume step2 may read verifiedstep1 artifacts. Partial trajectory no discard.
C38F: requires mainrelease bound to A scientificPASS+Bengineering/repeatabilityPASS; samebasez1 asB. All six actualdelta1 loaded from B independently, normalized maskRMS directions (zero ray means SKIPPED_C reason, not fabricate). Sharedbaselinepre/post atz1seed1; each6probesbeta.1/.2/.4±. Probe baseline/input hashes/noise sameBstep2. No scientificdecision embedded guessed; analyzer produces evidence and main gate files bind its results/code/sourcehashes. No automaticstageadvancement.
Smoke onebaseline fullF recorded engineering attempt, exact firstGparity, full G/D/E precision and actual feedback mapping, resources. Formal A/B/C counts exclude smoke/setup. Track observed invocationstart/completion counts even if call throws midG/D/E; don't infer counts from successful rows, don't default missing evidence. Resume counters include verifiedprior successes and recordedfailedattempts; failedattempt automatically rerunning OOM/monitor prohibited.

Resources reuse Task6 ResourceMonitor + evaluate_resources + resource_samplers; require all actualGPU/RAM/cgroup/disk fields before live start (PyTorch query failure cannot become0). Keep GPU1sec/RAM5sec and samplepre/post/cleanup, peakalloc/reserved recorded. Userthresholds exactly: preloadGPU<=1GiB, disk>=10GiB; smokepeakalloc<=35/NVML<=45; GPUused>75/free<20/reserved>65 stop, cgroupfree<10 stop/<20warn; Task6hostavailable<300/RSS>160/swapused>0 and cleanupgrowth stop. No independently weakened replacement. Forecast1.3*measuredsuccessbytes*remaining+5GiB checkperstage/per4samples; disk<5 stop and retaindata. Stage with no sizeestimate needs engineering smoke measuredbytes, not blind proceed. Complete snapshot missing/NaN/latchedfailure stops. Keepmonitor during gc/cacheclear, ensure stopwrites logs in ownrun notTask6source. Any cleanup failure recordsstop and no retry.

Atomic samples via established PrecisionSampleStore/publish_directory where possible (no replace success). Independenttemp and numberedfailedattempt preserved, no rmtree on evidence. ProcessLock preventssecondrunner. Strict resume validates config/source/model/VAE/framework/code/z0/mask/v0/noise policy and actualartifacthashes even when statusCOMPLETE, before returning. Empty/missing manifest entries cannot countassuccess. Requiredartifactset fixed. Save run_status countscompleted/failed/skipped andlastresources plusbindings on allfailures including load/serialize/monitor. Per-sample save/del/gc; close allmmap explicitly. No accumulatedoutputs. Track outputfile sizes not guessed4GBbudget.

TDD: real CLI wired through injected testloader; Acounts/nativeparity, Bownfeedback/noisereuse andGparity, Cfullsixrays/betas/gatepreconditions, sourcehashshape failures, preload/smoke/rollingresource hardstops includingcgroup/host/swap, partialcleanup errors, strictresume/counters/completestatustamper, failedattemptpreservation/processlock/mmapclose. Fake CPU tests prove orchestration only, no GPUclaims. Run focused then full Task4-6 once on integratedbranch; report missing optionaldeps explicitly withoutinstall. Return root docs/task7-task2c-report.md CLI and artifactschema/usage for offlineanalyzer Task3 andlive Task4. Commit scoped runner/tests/report.
