# Task2c round4: four small residual fixes, fresh Luna xhigh

User explicitly requires Luna xhigh implementer and main reviewer. Worktree C:/Users/hongy/Desktop/Plan/.worktrees/task7-fp32-feedback, project cosmos_umi_capstone. Read this brief first; do not read the whole conversation/plan. No subagents, GPU, remote/upload, environment changes, old Task4-6 source edits. Scope only experiments/run_umi_task7_experiment.py, its test file and docs/task7-task2c-report.md. Current frozen runner25bf5a9 has38 tests; main-reviewed core runtime/encoder unchanged. This is a scoped repair, NOT a rewrite or new orchestration design.

1. In _run_live smoke feedback.step exception handler, do NOT replace error.capture with live_observed_counts (stillzero until step returns). Approved FeedbackRuntime already attaches attempted G/D/E and partial diagnostics on exception. Preserve original capture, derive live_observed_counts from it, add boundary metadata without discarding it. Test feedback.step raises with capture G1/D1/E0 plus diagnostic marker: root observed counts and error capture remain correct. Existing post-return gate counts should still pass.

2. Setup observer restore falsely claims exact ownership: _install_setup_encoder_observer uses setattr(owner,'encode',bound_method) even when encode was class-defined; this leaves a new instance shadow. Snapshot __dict__ presence/value for tokenizer encode AND factory loader, then restore slot or remove shadow as appropriate, using existing patterns in approved encoder/runtime if useful. Test class method/no instance slot success+exception; existing instance override preserved. Do not silently continue to a real production G when observer installation/restoration failed. Test fixture factories without a loader may remain explicitly marked unavailable (not real setup evidence).

3. setup_evidence.json at root is overwritten every stage/resume. Persist it stage/attempt-scoped (like newly fixed monitor directories), and refer to that evidence from status/report. Keep observed setup count separate from formal counts; no guessed count. Preserve records from smoke,A,B,C, including failures. Test two stage setup snapshots both remain and identical-stage retry doesn't overwrite. No GPU calls are needed for tests.

4. COMPLETE resume currently rejects any failed_attempts, including legitimate interrupted RUNNING attempts that the runner supports resuming. Recompute observed counts including verified preserved interrupted attempts, plus successful sample counts; completed counts only successful planned samples. Reject genuine FAILED/RESOURCE_STOP automatic retry, malformed/duplicate/unbound attempts and inconsistent totals. Do not simply clear failed_attempts to pass. Add test interrupt with nonzero capture count -> explicit resume completes -> COMPLETE resume is a verified no-op with unchanged successful artifact bytes and no executor/factory calls. Keep existing contract/counter/duplicate tamper tests.

Use bundled local Python C:/Users/hongy/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe; run tests from project/experiments. No Torch locally; optional real-Torch skips are not failures to install away. Run focused runner tests and py_compile, update report with exact commands/counts, commit ONLY scoped files. Return status+commit+test summary+remaining concerns. Send a brief progress message after ~10 minutes if not done.

Main will review only these changes/new breakage. Other engineering evidence is already reviewed; final live source/precision/resource gates remain mandatory and have NOT run. No Task7 scientific samples exist yet.
